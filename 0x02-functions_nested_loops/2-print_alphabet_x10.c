#include "main.h"

/**
 * print_alphabet_x10 - print alphabet 10 times
 */


void print_alphabet_x10(void)
{
	int tentimes;
	int la;

	for (tentimes = 0; tentimes <= 0; tentimes++)
	{
		for (la = 'a'; la <= 'z'; la++)

			_putchar(la);
		_putchar('\n');
	}
}
